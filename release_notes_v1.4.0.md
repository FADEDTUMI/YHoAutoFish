# YHoAutoFish v1.4.0

本版本是一次重大升级：全新 v2 授权认证系统、全局埋点追踪、战绩分享、强制更新机制，同时切换至 Nuitka 编译打包以增强代码安全保护。

## 授权与安全

- 全新 v2 JWT 认证系统，支持 token 刷新与多主机故障自动切换。
- 新增 ECDSA-P256 离线 License 验证，断网场景下仍可正常运行。
- 设备指纹升级至 v2，引入 WMI 硬件查询与 HMAC 完整性校验。
- 授权存储新增 integrity HMAC 保护，防止本地数据篡改。
- 授权状态面板新增 License 到期时间与设备管理信息展示。
- 客户端新增 failover 机制，主服务器不可用时自动切换备用节点。

## 新功能

- 新增全局埋点追踪系统（EventTracker），支持离线 SQLite 队列与 WebSocket 实时上报。
- 新增战绩分享功能，支持截图保存与一键复制。
- 新增强制更新系统，支持 GitHub/Gitee 多源下载、分卷校验与 SHA256 验证。
- 新增客户端版本追踪与自动上报。
- 新增累计钓获自动售鱼功能，达到阈值后自动出售。

## 优化与重构

- 切换打包方案至 Nuitka standalone，代码经 C 编译保护，反逆向能力显著增强。
- 重构识别初始化流程，添加进度上报与性能优化。
- 拆分核心状态机模块，消除代码重复，提升可维护性。
- 优化视觉检测逻辑与结算界面识别算法。
- 优化月卡每日复位逻辑。

## 修复

- 修复授权复验时序问题与网络异常时缓存被误覆盖的问题。
- 修复自动出售确认按钮检测逻辑与结算界面阻塞处理。
- 修复多项全栈 Bug，提升整体稳定性。

## 升级说明

- 直接覆盖旧版本目录即可，原有 `config.json`、钓鱼记录、`auth_state.dat`、`auth_device.json` 会保留。
- 旧版 PyInstaller 打包的 `_internal/` 目录会在首次 Nuitka 版更新时自动清理（如通过自动更新升级）。
- 手动覆盖安装的用户可自行删除 `_internal/` 目录释放磁盘空间。

## 发布文件

- `YHoAutoFish-v1.4.0-windows.zip`：完整 Windows 发布包。
- `YHoAutoFish-v1.4.0-windows.zip.001`~`.003`：Gitee 国内源分卷包。
- `latest.json`：自动更新清单，需和发布包一起上传。
