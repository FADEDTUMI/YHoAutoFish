# YHoAutoFish v1.3.1

本版本重点修复来源验证误锁定、换机/挂失后的重新绑定体验、管理员后台会话卡死，以及自动更新时授权缓存保护不足的问题。建议所有 v1.3.0 用户升级。

## 主要更新

- 修复同 QQ、同设备重新绑定时旧授权 token 被提前失效的问题，避免用户误点生成绑定码后立即被锁定。
- 优化客户端来源验证弹窗，区分立即复验、生成新绑定码、管理员阻断、网络临时异常等状态。
- 修复挂失/换机被管理员批准后，客户端仍只能“立即复验”而不能生成新绑定码的问题。
- 静默复验失败不再反复强制弹窗，用户可以关闭弹窗、打开设置或正常退出程序。
- 网络错误、超时、502、429、TLS EOF 等临时异常不再覆盖本地授权缓存。
- 更新器新增保护 `auth_state.dat`、`auth_device.json` 及其临时文件，避免更新包误带授权文件时覆盖用户本机绑定数据。
- 发布包构建流程会拦截 `auth_state.dat`、`auth_device.json`、`records.json`，防止本地授权缓存或记录数据进入 release。
- 管理员后台新增统一请求层、请求超时、401/403 自动恢复、按钮 busy 释放和最近请求诊断，减少页面长时间无响应。
- 服务端 admin session 增加 `session_valid`、剩余时间、CSRF token 和明确错误原因，便于定位会话过期、CSRF 失效或反向代理异常。
- 机器人 `/yho help` 恢复并固定展示 `/yho change-device`、`/yho lost` 等关键自助命令；后台“菜单回复”调整为“菜单补充说明”。

## 升级说明

- 直接覆盖旧版本目录即可，原有 `config.json`、钓鱼记录、日志、截图、`auth_state.dat`、`auth_device.json` 会保留。
- 已完成换机/挂失审批的用户，升级后可在来源验证弹窗中生成新的绑定码并重新绑定。
- 如果某个用户已经被旧服务端清空 token，仍可能需要重新生成绑定码并在群内 `/bind`，或由管理员释放后再绑定。
- 服务端建议同步部署本版本 `server/`，否则客户端授权恢复和后台会话诊断无法完整生效。

## 发布文件

- `YHoAutoFish-v1.3.1-windows.zip`：完整 Windows 发布包。
- `YHoAutoFish-v1.3.1-windows.zip.001`、`.002` 等：Gitee 国内源分卷包。
- `latest.json`：自动更新清单，需和发布包一起上传。
